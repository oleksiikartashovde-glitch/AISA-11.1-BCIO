# AISA-11.1-BCIO

BCIO-centered knowledge and runtime repository for AISA 11.1 custom GPT.

## Structure

- `knowledge/` — ontology resources, semantic indices, and matching files
- `system/` — execution logic, normalization, output control, and runtime contracts
- `apease/` — APEASE reference layer
- `docs/` — user documentation
- `changelog/` — version history
